import history from '../shared/history';
import { Button } from 'primereact/button';

export const Home = () => {
    const styles = {
        display: 'flex',
        justifyContent: 'space-between',
        marginTop: '45vh',
        marginLeft: '40%',
        width: '20%',
    }

    return (
        <div style={styles}>
            HOME
        </div>
    )
}